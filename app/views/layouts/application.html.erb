<!DOCTYPE html>
<html>
  <head>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <meta charset="utf-8">
    <title><%= content_for(:title) || "The Gossip Project" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <link rel="manifest" href="/manifest.json">
    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <%= javascript_include_tag %>
  </head>
  <body>
    <header>
  <span>
    <a class="logoLink" href=<%= root_path %>>
      <img src="/assets/logo.png" alt="The Gossip Project" height="40", width="auto">The Gossip Project
    </a>
  </span>

  <span style="float:right;">
    <%= link_to "Home Page", root_path, class: "navButton" %>
    <%= link_to "Team", team_path, class: "navButton" %>
    <%= link_to "Contact", new_contact_path, class: "navButton" %>
    <% if session[:user_id] %>
  <%= button_to "Déconnexion", session_path(session[:user_id]), method: :delete %>
<% else %>
  <%= link_to "Connexion", new_session_path %>
<% end %>
  </span>
</header>
<% if flash[:notice] %>
  <div class="alert alert-success"><%= flash[:notice] %></div>
<% elsif flash[:alert] %>
  <div class="alert alert-danger"><%= flash[:alert] %></div>
<% end %>
    <%= yield %>
  </body>
</html>
